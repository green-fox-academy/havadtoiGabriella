public class TrueOrFalse {
  /*Create a static method in your class which has two integer parameter
The method should examine whether the first parameter is greater than the second one. It should return true or false.
Follow these steps:
Add a new test case
use the assertTrue to test the result of the created method
use the assertFalse to test the result of the created method
Run them
Fix your code if needed
   */
  public static boolean isGreater(int first, int second) {
    if (first > second) {
      return true;
    } else {
      return false;
    }
  }
}
